apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
    name: manual-storage
provisioner: k8s.io/minikube-hostpath  # Use the minikube hostpath provisioner
reclaimPolicy: Retain                # Options: Delete or Retain
volumeBindingMode: Immediate  
